---
"@rockclaver/sandcastle": minor
---

Add an internal language/stack profile registry (`js-ts`, `flutter`, `dart`, `go`) and route the default scaffold through the `js-ts` profile. `sandcastle init` now generates profile guidance markdown plus `profiles.json` metadata under `.sandcastle/profiles/`.
